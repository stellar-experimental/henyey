# maxtps-optimize run — 2026-07-03, frontier mode

- Session: 65c209f0 · Instance: 246qf48ddgrjk (nsc 32×64) · Base commit: 75634eb1
- Network calibration (this instance): 72.6 Gbit/s aggregate pod-to-pod
  (iperf3, 4 streams; ~19 Gbit/s single-stream), RTT avg 0.121 ms (min 0.046,
  max 0.347). Tx-byte ceiling estimate: 2000 tx/s × ~200 B × 23 nodes × ~22
  peers ≈ 1.6 Gbit/s worst-case full-mesh flood ⇒ ≥45× throughput headroom;
  RTT floor per pull round-trip ~0.12 ms.
- Methodology: MissionMaxTPSClassic, 23-node tier-1;
  screen = short probe (MULT=60), accept gate = sustained (MULT=300, 5-min);
  --install-network-delay false. Accept: Δ > 5% sustained + parity green.
- **FINAL — Current best (sustained): 1560 tx/s** (re-verified on iter-14 stack) · Burst: 1673 tx/s · baseline was 1452/1525 · core same-instance sustained: 1526 → **henyey beats core by +2.2% sustained**
- Core reference (THIS instance, iter-5): **sustained 1526** (1526 pass, 1535 fail; 1470/1507 pass). Disk writes: core ~35-44 MB/s at 1507-1526 vs henyey ~150-158 MB/s at 1481 → **henyey writes ~4× core**; both henyey killer modes are henyey-specific (core survives the seq-120-128 window and shows no inclusion-tail death at 1526).
- Max iterations: 20 (frontier mode — ends on network-bound proof or cap)

## Prior context (do not re-chase)

Baseline expectations post-PR #3712: burst ≈1510 (pre-WAL-fix number, may
rise), sustained ∈ [1400, 1450). Core on prior instance: burst 1530 /
sustained 1522. Refuted dead ends: laggard per-node queues, leader-computation
divergence, tx-set validate-on-fetch cost, raw-network limits at current rates
(re-calibrate per instance), tx_queue_banned growth (benign). Already fixed:
WAL checkpoint inline stalls, nomination tx-set fetch starvation (#3712),
trigger timer busy-loop (#3691), flood budget staleness, churn engine re-dial
(#3710), de-load stack (#3706/#3709).

## Hypotheses

| # | hypothesis | evidence/instrumentation | burst | sustained | Δ vs best | status | notes / next |
|---|------------|--------------------------|-------|-----------|-----------|--------|--------------|
| 0 | baseline re-measure (post-#3712) + network calibration | mission runs + iperf3 | 1525 | 1452 | — | done | burst 1525 (was 1510 pre-#3712); sustained 1452 (was [1400,1450)); WAL fix lifted both |
| 1 | skip per-tx RPC rows on validators (database.store_rpc_data auto-profile) | #3712 forensics: WAL/db writes dominant residual; core has no per-tx SQL since v21 | ≤1518 (screen 1525 ×2 fail) | 1457 | +0.3% (noise) | rejected as lever | PR #3714 opened as operator-mandated feature (honest: no TPS gain). Attribution value: SQL per-tx write volume is NOT the binder post-#3712 |
| 2 | failure forensics at the sustained edge (existing telemetry — no new code) | artifacts-iter1-sustained + artifacts-base-sustained | — | — | — | done | **BINDER NAMED: maintenance-delete WAL storm.** First checkpoint (63) publish completes → publish_safe_lmin jumps 0→~62 → next synchronized 10s maintenance tick bulk-DELETEs ~62 ledgers of txsets/txresults blobs (+ txhistory rows on base) in single statements → WAL write lock held 21-28s (db_write_ctx=maintenance-delete elapsed_ms=28213; "Maintenance took too long" 21-27s on all nodes) → event loop DB-stalls 2-4s (scp_verifier/tx_demand), ledger closes 19-54s globally (seq 126/127), WAL passive checkpoint 28 pages=1.6s (disk saturated) → backlog wedge → loadgen TryAgainLater death. Same signature on base (maintenance 12s + close-persist 12s at seq ~120-127). SSC sets AUTOMATIC_MAINTENANCE_PERIOD=10/COUNT=50000 for core too — core is immune because v21+ core has no SQL tx tables to purge |
| 3 | chunk maintenance deletes (bound WAL lock holds to ≤2 ledgers/stmt + 25ms pauses) | iter-2 forensics: maintenance-delete held WAL lock 21-28s | screen 1525 PASS | 1472 | +1.4% | rejected as lever (kept as bug fix, PR below) | commit 7b36f59b. Storm fixed: first delete wave survived (was fatal at 1465 on base); maintenance-delete holds 28s→≤7.7s (residual = lock-wait under new binder). Sustained gate 1525 failed pre-storm (under-filled ledgers = ingest ceiling). NEW binder at seq ~123-127: scp-persist-write slot-keyed holds 7-21s + scp-persist-purge 8.4s; event-loop DB-lock stalls (consensus_tick_fetch_drain 13s, scp_verified 2.4s, scp_timeout 2.1s) → "Consensus stuck → out-of-sync" → loadgen TryAgainLater at pending_age=1 |
| 4 | trigger-skew compensation (arm 5s − (now − lastTrigger)) | core HerderImpl::ledgerClosed; old −1.1% burst rejection predates WAL fix | | | | pending | retest under sustained gate |
| 5 | nomination pipeline latency: staleness-safe prebuild of build_value | 130-250ms build + ~150ms fetch serialized in 1s round-1 budget | | | | pending | naive prebuild = #3638 liability (revert 1afc810a) |
| 6 | ~~parallel apply for disjoint-account classic payments~~ | — | | | | dropped | operator directive 2026-07-03: do not pursue apply-path optimizations (evidence agrees: apply ≈200ms of 5s cadence, closes fast until non-apply stalls) |
| 7 | SCP intake/verify contention | ~90ms verify queueing at ballot bursts | | | | pending | |
| 8 | flood path outbound batch coalescing | PR #3701 diagnostic | | | | pending | |
| 9 | queue/ban semantics under backlog (open-loop applied-rate) | #3705 audit | | | | pending | |

## Iteration log

- **Iter 4 (diagnostic, host I/O attribution)**: reran 1481 sustained with /proc-based
  per-process + vda disk sampling. The seq-123-127 stall did NOT reproduce; instead the
  run died at seq ~57 via the OTHER death mode: per-account inclusion tail
  (pending_age=2 fatal reject; ledgers carried ≥ target so throughput kept up).
  Two independent killers at the edge: (a) seq~120-128 I/O-burst stall
  (checkpoint publish + first delete wave + level-3 spill window; scp-persist-write
  waits 7-21 s), (b) inclusion-tail wedge — selection excludes ~40-54 txs/ledger
  as sequence-gapped (`seq != expected → break` in select_transactions_with_starting_seq),
  i.e. a tx lost/late in flooding wedges its account until the loadgen's next
  submission fatal-rejects (one-tx-per-account queue parity). Steady-state disk:
  **100-180 MB/s VM-wide (~6.5 MB/s/node) at 1481** — no headroom for bursts.
- **Iter 5 (core reference, same instance + same sampler)**: core sustained edge **1526**
  on this instance (henyey 1472 → real 3.5% henyey-specific gap). Disk write steady-state:
  core 35-44 MB/s @ 1507-1526 vs henyey 150-158 MB/s @ 1481 → **~4× write amplification
  is henyey-specific**. Next: per-table byte decomposition of a node DB (iter 6).
- **Iter 6 (DB byte decomposition)**: --keep-data failing step at 1481, pulled a
  validator stellar.db (589 MB after ~11 min; buckets only 21 MB). Per-table (dbstat):
  **ledger_close_meta 409 MB (70%)**, storestate (SCP persist) 80 MB, txsets 55 MB,
  txresults 17 MB, txhistory ~0 (iter-1 gate works). ledger_close_meta = full
  LedgerCloseMeta XDR per ledger, serves only RPC getLedgers, serialized on the event
  loop (~4.5 MB/ledger), and NEVER pruned without rpc retention → unbounded disk leak
  on non-RPC validators.
- **Iter 7 (lever)**: gate LedgerCloseMeta serialization+storage under store_rpc_data
  (commit 4a0ff1cd, PR #3716). Write volume: 150 → ~55 MB/s (core ~40) — WORKS. Sustained
  edge unchanged (<1479; screen 1546 failed = above burst ceiling). Verdict: rejected as
  >5% lever, landed as production fix (unbounded ledger_close_meta growth = disk leak).
  Remaining binder = inclusion tail (not I/O).
- **Iter 8 (instrumentation, commit 31ff1b3b)**: maxtps_tail diagnostics. Findings at
  1479: stranded txs show **sent_to=22/22** (origin adverted everywhere — loss is
  demand/response side); receiver demand queues FULL at max_ops=14790 (~2 ledgers deep),
  dropping 100-240 hashes per 200 ms advert batch on hot peers. Demander dedup happens at
  pop time (adverts arrive 22× redundant); unfulfilled demands recycle as backoff retries
  that consume the per-period demand budget.
- **Iter 9 (lever)**: reliable demand fulfillment — handle_flood_demand
  dropped the REST OF THE BATCH on a full outbound channel (core never drops; its
  responses queue in the per-peer write queue). Now deferred per-peer (cap 4000) and
  drained each flood tick. Regression tests: full-channel deferral+flush, stale-entry
  skip. Edge unchanged (<1479, same account-0 wedge; 1227 overflow events persist) —
  necessary but not sufficient; kept on the stack.
- **Iter 10 (lever, PARITY GAP)**: demand_status was missing stellar-core's
  TxDemandsManager::demandStatus:71-74 check — never demand a tx already queued or
  banned. Without it every fulfilled hash cycled as RetryLater through ~21 other peers'
  queues for the full 60 s retention, permanently saturating demand queues (full at
  max_ops=14790, 100-240 drops/batch) and starving new-tx demands. Fix + parity test.
  **RESULT: sustained edge 1472 → 1516 (+3.0%); overflow events 1227 → 0; core gap now
  0.7% (1516 vs 1526). PR #3717 (iters 8+9+10).** 1526 step ran full 5.8 min before
  failing (near-miss, residual thin tail: 1160 stranded-tx lines in the passing 1516
  run — next binder).
- **Iter 11 (ladder on iter-10 stack)**: burst 1525 → **1673** (+9.7%; the old "burst
  ceiling" was the demand-queue saturation). Sustained ladder: 1526 PASS (= core),
  1560 PASS (**beats core +2.2%**), 1600 FAIL — but the 1600 death is the warmup
  lottery again (account-0 wedge at ledger ~7 while steady-state ledgers carried
  8600-10200 txs at ~200 ms close). Sustained edge ∈ [1560, 1600); steady-state
  capacity likely ≥1650.
- **Iter 12 (instrumentation)**: demander-side unfulfilled-demand log (peers_demanded
  vs never-demanded) to attribute the residual warmup-window strand.

## Summary

- **Baseline → final: burst 1525 → 1673 (+9.7%), sustained 1452 → 1560 (+7.4%)**;
  core same-instance sustained 1526 → henyey +2.2% over core. 15 iterations used
  (5 diagnostics, 7 code levers of which 5 landed, 3 measurement/ladder).
- PRs: #3714 (store_rpc_data validator profile, merged), #3715 (chunked maintenance
  deletes, merged), #3716 (skip LedgerCloseMeta on validators, merged), #3717
  (tx-pull stranding: owned/banned demand discard [core parity gap] + deferred
  demand responses, merged — THE breakthrough: sustained 1472→1516, burst
  1525→1673), #3718 (same-peer re-demand + tail diagnostics, open).
  Follow-up issue: #3719 (residual warmup wedge).
- **Bottleneck attribution: network-bound NO.** Raw network has ≥45× byte headroom
  (72.6 Gbit/s pod-to-pod calibrated vs ~1.6 Gbit/s needed; RTT 0.121 ms). The final
  binder at 1600 sustained is a compound HARNESS-INTERACTION edge case, not capacity:
  (1) sole-advertiser tx strand at network warmup (single lost demand round → tx
  applies ~2 ledgers late), compounded by (2) henyey's pipelined post-close queue
  cleanup (a submission landing in the ~60 ms window after close sees a
  just-consumed pending → TryAgainLater → PayPregenerated insta-fails per #3638
  parity). Steady-state throughput at 1600 was healthy in every failing run
  (8600-10200-tx ledgers, ~200 ms closes, no WAL pressure, disk ~55 MB/s vs core
  ~40). Filed as #3719.
- Top remaining hypotheses: fix #3719 part 2 (admission-time stale-pending
  self-heal — likely unlocks 1600+ sustained); nomination prebuild (untouched,
  seeded #5); SCP intake/verify contention (#7); WAL/persist further off-path
  (storestate 80 MB/11 min is the next write hog after the ledger_close_meta fix).
- Instance drift note: all numbers same-instance (246qf48ddgrjk); re-baseline
  before comparing future campaigns.
